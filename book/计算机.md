人工智能的前提是思考人的大脑是如何从经验中提取信息进行思考的。

人工智能的前提是思考人的大脑是如何从经验中提取信息进行思考的

深度学习
半监督学习 semi-supervised learning
定义：半监督学习结合了监督学习和无监督学习的特点，通常只有少量带标签的数据和大量无标签的数据，模型在带标签数据的帮助下，利用无标签数据中的结构信息来提高学习效果
应用： 半监督学习在标注数据稀缺的情况下非常有用，如医疗影像分析、文本分类等领域
示例：在训练一个深度学习模型进行图像分类时，只有一小部分图像有标签，而其他图像没有标签。半监督学习可以利用未标记的图像来帮助模型提高性能。

强化学习（reinforcement learning)
定义： 强化学习是一种基于行为和奖励的学习方法，模型（智能体）通过与环境互动，调整策略，以达到最优的决策效果。

总结：
深度学习系统通常是通过 监督学习（主要），并结合 无监督学习 和强化学习，根据具体的任务和数据形势进行学习。
监督学习常用于有明确标签的数据集，常见于分类、回归等任务；无监督学习和半监督学习用于处理没有标签的数据，强化学习则用于动态环境中的决策问题。

深度学习架构的五个主要类型

1. 神经网络（CNN

监督学习

回归: 预测一个连续值

准备： 
1. 模型类型 
2. 模型框架
3. 数据集
4. 搭建环境，使用学习框架

模型训练
1. 编写代码
2. 定义损失函数
3. 选择优化器
4. 编写训练循环
5. 监控训练过程

机器学习的核心是数据驱动的学习过程。数据包括输入数据（特性）和相应的输出标签（对应的目标），用于训练模型和评估模型的性能

人工神经网络（artificial neural networks ,ann) 是一种受生物神经网络启发的计算模型，广泛应用于机器学习和人工智能中，用于解决各种问题，如分类、回归、图像识别、语音处理等。神经网路模拟了大脑中神经元之间的连接与信号传递，能够自动从数据中学习特征并进行预测。

人工神经网络的基本结构
一个典型的人工神经网络由多个神经元（也叫节点）组成，神经元之间 通过 权重 (weight) 相连接，形成不同层次的网络。神经网络的基本结构包括：
1. 输入层（input layer): 接受外部数据作为输入，每个神经元对应一个输入特征
2. 隐藏层（hidden layers): 输入通过隐藏层处理，隐藏层由多个神经元组成，它们接受前一层的输出并进行进一步计算，网络可以有一个或多个隐藏层。
3. 输出层 (output layer)
输出层的神经元负责输出网络的预测结果或分类标签。

每个神经元接收前一层的信号，将这些信号加权求和后，通过激活函数进行非线性交换，输出结果传递到下一层。

神经元的工作原理
每个神经元的工作方式可以用以下步骤描述：
1. 加权和： 神经元将输入信号与权重相乘并加和，得到一个加权和。比如，对于一个输入 
$ x_1, x_2, \ldots, x_n $ 和相应的权重 $w_1,w_2,\ldots,w_n $, 神经元的输入可以表示为：
$$z=w_1 x_1 + w_2x_2+\cdots+w_nx_n+b$$
其中$b$是偏置项(bias)

2. 激活函数：神经元将加权和 $z$ 输入到激活函数中，激活函数负责引入非线性因数，使得网络能够拟合更复杂的模式。常见的激活函数包括：
   - Sigmoid(乙状结肠): 输出范围在0到1之间，适用于概率预测
   - ReLU (rectified Linear Unit 线性整流函数): 常用于深度学习，输出未 $\max(0,z)$，加快训练速度。
   - Tanh双曲正切: 输出范围在 -1 到1之间，适用于一些特定问题。
- 3. 输出结果：激活函数的输出将作为该神经元的输出，并传递给下一层神经元。

神经网络的学习过程
神经网络通过学习数据的模式来调整权重，从而使得预测结果尽可能准确。学习过程通常包括以下几个步骤：
1. 前向传播（forward Propagation): 输入数据经过各层神经网络的处理，直到输出层得到最终的预测结果。
2. 计算误差（Loss calculation): 通过比较网络的输出与实际标签之间的误差，计算损失函数（loss function). 常用的损失函数包括均方误差（MSE）和交叉熵损失（Cross entropy).
3. 反向传播（backpropagation): 通过梯度下降等优化方法，计算每个权重的梯度 ，并根据梯度更新权重。通过反向传播算法，神经网络可以逐层调整权重，以减少误差。
4. 优化算法： 常用的优化算法和梯度下降（Gradient descent)、adam等，帮助网络在训练过程中最小化损失函数，提高模型性能。

神经网络的应用
人工神经网络在众多领域有广泛的应用，主要包括：
- 图像识别： 通过卷积神经网络（CNN）处理图像数据，广泛应用于面部识别、物体检测、图像分类等。
- 语音识别：将语音信号转换为文本，常用于语音助手（如Siri、Google Assistant）等。
- 自然语言处理：用于文本分类、情感分析、机器翻译、语音生成等。
- 推荐系统：基于用户行为数据预测用户可能感兴趣的商品或内容。
- 游戏和自动控制：强化学习与神经网络结合，可以用于训练智能体在复杂环境中进行决策（如 AlphaGo,自动驾驶等）

神经网络的优势与挑战

优势
- 强大的拟合能力：神经网络能够学习复杂的非线性关系，适应各种复杂模式
- 自学习：神经网络通过数据自动调整权重，无需手动编程
  
  挑战
  - 训练时间长： 深层神经网络通常需要大量的数据和计算资源进行训练
  - 过拟合： 当训练数据不够多时，神经网络在测试集上表现差
  - 可解释性差： 深度神经网络是黑箱模型，难以直观理解其内部工作原理
总结来说，人工神经网络是一种模仿生物神经系统的计算模型，通过对大量数据的学习来做出预测和决策。随着深度学习技术的发展，神经网络在许多领域得到了突破性的应用。


计算机系统的应用：

文件文件夹的管理:
文件和文件夹的抽象，可以方便的把不同类型的文件放在不同的地方。

就像家里的衣柜，被子放一个柜子，衣服放一个柜子，裤子放一个柜子，枕头放一个柜子，可以分类浏览。

查找程序： find  可以按照文件名字，查找文件，或者文件属性查找文件

- type 按照类型查找 f 文件 d 目录 l 链接

文件拷贝 cp ，实际生活中，你要花钱购买新的一样的东西，在计算机中，使用拷贝就可以了。

捆绑，包扎  tar

画画 ： 你要拿出画板工具，还有笔，颜料进行绘画，计算机中也做了绘画软件。

查看网络信息： 你家就相当于邮寄地址，邮局通过 地址信息就行邮件 寄送。

写字本：vim ，能方便的操作写过的文字。

inoremap kj <ESC> 不使用esc 键，而使用 kj 键来切换模式，因为 esc 键有点远了。

网络编程，要使用的工具，就是 服务器，因为客户端时展示样式的，而服务器是运行结果的，通过网络通信。javascript 也能执行脚本呀，处理小的应用也是可以的。

编程语言。
编程语言，变量，常量，用来表示客观世界中的产品，工具。 用 表达式，函数，表示 产品的能够运行的行为，产品能用作某种行为，还是由于产品本身的特点。

流程控制，在不同的条件下，控制 流程的流出方向。是为了逻辑更科学有道理。

一般是 判断流程 ，还有重复流程，异常六流程。

产品的数据结构，有 列表，元组，  
常用的产品，产品的属性
文件： 读取文件，写入文件
异常：追踪异常产品，进入异常处理流程 
面向对象编程： 类和对象，复用产品。继承：产品更新演化

模块和包： 方便调用第三方产品

使用 scoop全局切换 python 版本
scoop reset python39
切换到 python 3.9
scoop reset python310
切换到pyhon3.10

或者使用 pyenv 工具
 pyenv install 3.10.12
  pyenv install 3.9.13
  pyenv global 3.10.12     # 设置全局版本
  pyenv local 3.9.13       # 当前目录使用 3.9
  pyenv versions           # 查看安装的版本
  
git 东西的版本处理

1. 处理一些个人配置
git config --glabal user.name " 你的 名字" 东西的创作者，共创者
git config user.email 东西的联系邮件
git config list
查看配置了哪些东西

分支管理（东西版本管理）
git branch -r -d 分支名  ,git push 删除远程分支
git branch -D origin/分支名 删除本地繁殖

子模块操作
git submodule add 添加子模块到产品中
git submodule update 拉去子模块产品

.gitignore 文件，彻底不跟踪某个文件
git rm --cached 文件路径

数据库产品，表，数据库是 比文件柜更精密的产品，就像生活中的表格一样，更精细操作数据，节约空间。

Redis : 这个好像不是表啊，往下记录下
REmote DIctionary Server 远程字典服务，可以通过网络访问、存储和检索键值对数据。

概述： Redis 是速度非常快的非关系型（NO SQL） 内存键值数据库，键的类型只能是字符串，但 值的 类型可以是 字符串，列表，集合，散列表，有序集合。

安装 redis
sudo apt install redis-server
设置开机启动
sudo systemctl enable redis-server
检查运行状态
sudo systemctl status redis-server

测试是否运行
redis-cli

```
设置键值对
字符串
set hello world
get hello
del hello

列表，插入列表
rpush list-key item
rpush  list-key item2
lrange list-key  0 -1
lindex list-key 1
lpop list-key

set 集合
sadd set-key item 
smembers set-key

```

网络
如果给互联网供应商提供钱，购网络服务，那们就可以连到互联网。我们也可以通过购买 交换机，连间单一域的本地网，或者 更大的跨域网，则可以通过路由器。然后我们可以通过 IP 地址，或者 配置域名服务的机器，访问其他机器的服务。一般网络服务 有 
web 网页服务 ，ftp 文件传输服务，邮件发送服务等等。。

确实，通过这么多的服务，这个只是最顶层的应用层，还要通过分层，分片传输 分片的网络数据

有 应用层
传输层 tcp udp
网络层 ip
链路层 以太网

未来趋势
人工智能与自动化
趋势：AI将像互联网一样渗透所有行业


